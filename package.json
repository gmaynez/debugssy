{
  "name": "debugssy",
  "displayName": "Debugssy",
  "description": "MCP server for VS Code debugging with tools for breakpoints, debug control, and variable inspection",
  "version": "1.4.0",
  "publisher": "gamag",
  "author": {
    "name": "Guillermo Garcia Maynez Rocha"
  },
  "license": "Apache-2.0",
  "repository": {
    "type": "git",
    "url": "https://github.com/gmaynez/debugssy.git"
  },
  "bugs": {
    "url": "https://github.com/gmaynez/debugssy/issues"
  },
  "homepage": "https://github.com/gmaynez/debugssy#readme",
  "keywords": [
    "debugging",
    "ai",
    "mcp"
  ],
  "icon": "assets/debugssy_logo.png",
  "engines": {
    "vscode": "^1.90.0"
  },
  "categories": [
    "Debuggers",
    "Other"
  ],
  "activationEvents": [
    "onStartupFinished"
  ],
  "main": "./out/extension.js",
  "contributes": {
    "configuration": {
      "title": "Debugssy",
      "properties": {
        "debugssy.mcp.enabled": {
          "type": "boolean",
          "default": true,
          "description": "Enable the MCP server for debugging tools"
        },
        "debugssy.mcp.port": {
          "type": "number",
          "default": 3000,
          "description": "Port for the MCP server to listen on (localhost only)"
        },
        "debugssy.automationLevel": {
          "type": "string",
          "enum": [
            "assisted",
            "full"
          ],
          "default": "assisted",
          "description": "Control automation level: 'assisted' (user controls flow, AI assists) or 'full' (AI has full control)",
          "enumDescriptions": [
            "Assisted mode: AI can set breakpoints and inspect, but user controls execution flow via VS Code UI",
            "Full mode: AI has complete control over debugging session including starting, stopping, and stepping"
          ]
        },
        "debugssy.waitForBreakpointTimeout": {
          "type": "number",
          "default": 5000,
          "minimum": 1000,
          "maximum": 300000,
          "description": "Default timeout in milliseconds for wait_for_breakpoint tool (1s to 5min, default: 5s). Can be overridden per-call."
        },
        "debugssy.allowStepOperations": {
          "type": "boolean",
          "default": false,
          "description": "Enable step operations (step_over, step_into, step_out) in full automation mode. Note: For AI debugging, setting strategic breakpoints and using 'continue' is generally more efficient."
        },
        "debugssy.maxExpressionLength": {
          "type": "number",
          "default": 100,
          "minimum": 20,
          "maximum": 400,
          "description": "Maximum allowed length for expressions in evaluate_expression tool. Helps prevent prompt injection attacks by limiting code execution length. Default: 100 characters, Range: 20-400."
        },
        "debugssy.expressionValidationLevel": {
          "type": "string",
          "enum": [
            "strict",
            "moderate",
            "permissive",
            "disabled"
          ],
          "default": "moderate",
          "description": "Expression validation strictness level (threshold-based like log levels)",
          "enumDescriptions": [
            "Strict: Only whitelisted safe functions allowed automatically. All others require approval (maximum security).",
            "Moderate: Whitelisted functions + common getter patterns allowed. Unknown functions require approval (recommended balance).",
            "Permissive: Most operations allowed automatically. Only dangerous system operations require approval (minimal interruptions).",
            "Disabled: No validation - all expressions execute without approval (not recommended, use only in fully trusted environments)."
          ]
        }
      }
    },
    "commands": [
      {
        "command": "debugssy.startServer",
        "title": "Debugssy: Start Server"
      },
      {
        "command": "debugssy.stopServer",
        "title": "Debugssy: Stop Server"
      },
      {
        "command": "debugssy.restartServer",
        "title": "Debugssy: Restart Server"
      }
    ]
  },
  "scripts": {
    "vscode:prepublish": "npm run package-extension",
    "compile": "npm run check-types && node esbuild.js",
    "check-types": "tsc --noEmit",
    "watch": "npm-run-all -p watch:*",
    "watch:esbuild": "node esbuild.js --watch",
    "watch:tsc": "tsc --noEmit --watch --project tsconfig.json",
    "package-extension": "npm run check-types && node esbuild.js --production",
    "pretest": "npm run compile",
    "test": "vitest run",
    "test:watch": "vitest watch",
    "test:ui": "vitest --ui",
    "test:coverage": "vitest run --coverage",
    "lint": "eslint src",
    "format": "prettier --write \"src/**/*.ts\" \"*.{js,json,md}\"",
    "format:check": "prettier --check \"src/**/*.ts\" \"*.{js,json,md}\"",
    "package": "vsce package",
    "prepare-publish": "node scripts/prepare-marketplace.js swap",
    "restore-publish": "node scripts/prepare-marketplace.js restore",
    "publish-status": "node scripts/prepare-marketplace.js status",
    "publish-marketplace": "npm run prepare-publish && npm run package && npm run restore-publish"
  },
  "devDependencies": {
    "@eslint/js": "^9.39.0",
    "@types/express": "^5.0.5",
    "@types/node": "^24.9.2",
    "@types/vscode": "^1.90.0",
    "@typescript-eslint/eslint-plugin": "^8.46.2",
    "@typescript-eslint/parser": "^8.46.2",
    "@vitest/coverage-v8": "^4.0.6",
    "@vitest/ui": "^4.0.6",
    "@vscode/vsce": "^3.6.2",
    "esbuild": "^0.25.12",
    "eslint": "^9.39.0",
    "npm-run-all": "^4.1.5",
    "ovsx": "^0.10.6",
    "prettier": "3.6.2",
    "typescript": "^5.9.3",
    "vitest": "^4.0.6"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.21.1",
    "express": "^5.1.0",
    "zod": "^4.1.12"
  }
}
